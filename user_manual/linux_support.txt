Support for Linux: Ubuntu

*** Perhaps not necessary
{
* Install Arduino IDE
* Add Adafruit channel
* Install Adafruit SAMD boards
}

* Fix access to serial ports
** 1) Need to apply at every reboot
sudo chmod 666 /dev/ttyS0
** 2) Or permanent solution
sudo gpasswd --add ${USER} dialout
